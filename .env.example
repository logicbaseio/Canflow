# Neon Postgres connection string (Neon dashboard → Connection Details → "Pooled connection")
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# Optional: Resend API key to send beta-tester invite emails (https://resend.com).
# Without it, invites still work — the app returns a shareable link instead.
RESEND_API_KEY=""
